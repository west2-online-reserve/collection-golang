创建切片的方式：
1.直接用make()函数给定一个切片，包括切片的长度，指针，容量
2.声明一个切片，通过append向切片中添加元素
3.通过append将两个切片组合成一个新的切片
4.通过循环遍历向切片中添加连续的元素



创建map的方式：
1.通过make(map[类型]类型){ }创建  ：var a = make(map[类型]类型){ }
2.通过var声明并初始化：var a = map[类型]类型{ }
3. a := map[类型]类型{ }
