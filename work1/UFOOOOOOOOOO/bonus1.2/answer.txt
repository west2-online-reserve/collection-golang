切片不限制元素个数，个数可以变动。数组限制元素个数，个数不可以变动。
创建切片可以使用:
1、make 
2、var slice []int=[]int{1,2,3,4,5}
3、slice := []int{1, 2, 3, 4, 5}
4、slice = append(slice,a)
5、slice := make([]int,5,10)
创建map：
1、myMap := make(map[string]int){
   }

