**切片和数组的区别**
数组长度固定，是一块连续的内存数据。
切片是一个包含 3 个字段（指向底层数组的指针，长度，容量）的结构体，

**创建切片有几种方式**
- []int{}
- make函数 make([]int, len, cap)
- 从已有的数组或切片中切 arr[begin:end]
- 声明一个 nil 切片 var s []int

**创建 map**
- map[int]int{}
- make(map[int]int) 空的，非nil；make(map[int]int, cap)
- 声明一个 nil map var m map[int]int